# Hello World Node.js Application

This is a basic Node.js application with an HTTP endpoint that returns a greeting response.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Start the server:

```bash
npm start
```

The server will start on port 3000 by default.